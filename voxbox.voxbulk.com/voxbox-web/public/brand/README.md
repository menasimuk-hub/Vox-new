# Brand assets

Logos are synced from `voxbulk-api/logos/` via `npm run sync:brand` (runs on predev/prebuild).

Expected files: `logo-black.png`, `logo-white.png`, etc.

If this folder is empty, add PNG/SVG files to `voxbulk-api/logos/` and re-run sync.
