# Local dev only — recovery/follow-up modules are excluded from production builds (import.meta.env.DEV).
# Copy to .env for local development; production uses .env.production only.

VITE_API_BASE_URL=https://api.voxbulk.com
VITE_PUBLIC_SIGNIN_URL=https://voxbulk.com/signin
VITE_PUBLIC_APP_URL=https://voxbulk.com

VITE_PROXY_API_TARGET=http://127.0.0.1:8000

